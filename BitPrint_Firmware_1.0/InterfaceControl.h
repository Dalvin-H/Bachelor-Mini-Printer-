//This class will contain all methods for controlling the interface screen + user input (rotary knob / reset button)

#ifndef INTERFACECONTROL_H
#define INTERFACECONTROL_H

#include <Arduino.h>
#include "Config.h"

class InterfaceControl {
  private:

  public:

};

#endif